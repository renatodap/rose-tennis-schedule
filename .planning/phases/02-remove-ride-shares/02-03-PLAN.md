# Phase 02-03: Create DB Migration to Drop Ride Share Tables

## Objective
Create a Supabase migration to remove the ride sharing database tables.

## Context
@supabase/migrations/20250117_ride_shares.sql

## Tasks

### Task 1: Create drop ride shares migration
- **Type:** create
- **Files:** `supabase/migrations/20251203_drop_ride_shares.sql`
- **Action:** Create a new migration file that:
  1. Drops `ride_requests` table first (has FK to ride_offers)
  2. Drops `ride_offers` table
  3. Uses `IF EXISTS` to be idempotent
- **Verify:** Migration file exists with correct SQL
- **Done:** [ ]

## Verification
- [ ] Migration file exists at correct path
- [ ] SQL drops tables in correct order (children before parents)
- [ ] Migration is idempotent

## Success Criteria
- Migration ready for production
- Ride share database cleanup automated

## Output: SUMMARY.md
Document completion status.
