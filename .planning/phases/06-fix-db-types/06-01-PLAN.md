# Phase 06-01: Audit and Fix Database Types vs SQL Schema

## Objective
Ensure TypeScript database types accurately reflect the actual SQL schema.

## Context
@lib/types/database.types.ts
@supabase/migrations/001_initial_schema.sql
@supabase/migrations/004_team_based_schema.sql
@lib/constants.ts

## Tasks

### Task 1: Compare types with SQL schema
- **Type:** audit
- **Files:**
  - `lib/types/database.types.ts`
  - `supabase/migrations/*.sql`
- **Action:**
  1. Read all SQL migrations to understand actual schema
  2. Compare with TypeScript types
  3. Document all mismatches:
     - Field names that differ
     - Types that don't match (e.g., quarter enum vs string)
     - Missing or extra fields
- **Verify:** Complete list of mismatches documented
- **Done:** [ ]

### Task 2: Fix User interface
- **Type:** edit
- **Files:** `lib/types/database.types.ts`
- **Action:** Update User interface to match SQL exactly:
  - Ensure field names match (phone vs phone_number)
  - Ensure types match SQL types
- **Verify:** User interface matches users table
- **Done:** [ ]

### Task 3: Fix ClassSchedule interface
- **Type:** edit
- **Files:** `lib/types/database.types.ts`
- **Action:** Update ClassSchedule interface:
  - SQL uses `quarter quarter NOT NULL` and `year INTEGER` separately
  - TypeScript uses `quarter: string`
  - Align to match SQL or document intentional deviation
- **Verify:** ClassSchedule interface is accurate
- **Done:** [ ]

## Verification
- [ ] All interfaces audited against SQL
- [ ] Key mismatches fixed
- [ ] `npx tsc --noEmit` passes

## Success Criteria
- TypeScript types accurately reflect database schema
- No surprises at runtime due to type mismatches
