# Phase 03-02: Remove Badge Types and Create DB Migration

## Objective
Remove badge types and create migration to drop badge tables.

## Context
@lib/types/database.types.ts
@supabase/migrations/20250117_badges.sql

## Tasks

### Task 1: Remove badge types from database types
- **Type:** edit
- **Files:** `lib/types/database.types.ts`
- **Action:** Remove `BadgeDefinition` and `UserBadge` interfaces and their table definitions from the `Database` interface
- **Verify:** Types file compiles without errors
- **Done:** [ ]

### Task 2: Create drop badges migration
- **Type:** create
- **Files:** `supabase/migrations/20251203_drop_badges.sql`
- **Action:** Create a new migration file that:
  1. Drops `user_badges` table first (has FK to badge_definitions)
  2. Drops `badge_definitions` table
  3. Uses `IF EXISTS` to be idempotent
- **Verify:** Migration file exists with correct SQL
- **Done:** [ ]

## Verification
- [ ] Database types have no badge references
- [ ] Migration file exists and is valid SQL
- [ ] `npm run build` passes

## Success Criteria
- Badge feature completely removed from codebase
- Database migration ready for production

## Output: SUMMARY.md
Document completion status.
